apiVersion:: v1
kind: Service
metadata:
  name: vote-ui
  labels:
    app: vote-ui
spec: 
  type: NodePort
  selector:
    app: vote-ui
  ports:
    - name: http
      port: 80
      targetPort: 80
      protocol: TCP